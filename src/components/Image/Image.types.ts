/**
 * Contains type of props for Image component
 * @property src - source of image to render
 * @property srcSet - srcSet prop for image
 * @property alt - alt value in image
 */
export type ImageProps = {
    src: string;
    srcSet: string;
    alt: string;
};
