/**
 * Contains type of props for Image component
 * @param src - source of image to render
 * @param srcSet - srcSet prop for image
 * @param alt - alt value in image
 */
export type ImageProps = {
    src: string;
    srcSet: string;
    alt: string;
};
